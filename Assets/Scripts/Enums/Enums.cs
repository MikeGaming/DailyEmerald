using UnityEngine;

public class Enums
{
    //don't set values to the enums, they'll handle themselves

    public enum ItemType
    {
        SWORD,
        AXE,
        MACE,
        SPEAR
    }

    public enum MaterialType
    {
        IRON,
        SILVER,
        GOLD
    }

    public enum MagicType
    {
        NONE,
        FLAME,
        FROST,
        LIGHTNING
    }
}
